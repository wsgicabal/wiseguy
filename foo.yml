helloworld:
  component: helloworld

notfound:
  component: notfound

pipe:
  component: pipeline
  config:
    apps: [ compress, helloworld ]

compress:
  component: gzip
  config:
    compress_level: 6

main:
  component: cascade
  config:
    apps: [ notfound, pipe ]
