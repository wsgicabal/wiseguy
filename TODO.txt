- Finish or delete web.app.

- Provide a way to specify a server.

- Provide a way to consume Paste entry points.

- Provide a mechanism to disuse entry points and point at a component
  directly.

- Much friendlier error messages when schema is not satisfied or there's a
  bug in a factory.

- Expand %(here)s or ${here} in configuration values.
